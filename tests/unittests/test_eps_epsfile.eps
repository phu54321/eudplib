function square();

const a = [
    square(1),
    square(2),
    square(3),
    square(4),
    square(5)
];

function square(x) {
    // const z = EUDArray(5);
    return x * x; // + z.k;
}

function constv_thing() {
    return a[0] + a[1] + a[2] + a[3] + a[4];
}
